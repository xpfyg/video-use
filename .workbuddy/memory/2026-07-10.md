# 2026-07-10

## 实现 classify_materials.py（素材动作分类标签）

- 新增 `helpers/classify_materials.py`：遍历中间产物 cache 的 `cache/frames/<素材>/`，
  每个素材目录按时间轴均匀抽 N 张图（默认 3），拷贝到 `cache/selected/<素材>/`，
  发给 Ollama 视觉模型（默认 `qwen3-vl:8b`）从候选标签里选一个，生成
  `material_labels.json` + `material_labels.md`。
- 冷饮默认标签：拿起产品 / 放下产品 / 开盖展示 / 饮料气泡特写 / 配料表 / 倒饮品动作。
- 标签归一与兜底：模型返回标签字段为空/非法时，先从原始文本（含 reason）找回，
  仍失败则标记 `needs_review=true`。支持 `--force` 重跑、`--dry-run` 仅抽帧、缓存命中跳过。
- Ollama 调用方式沿用 `analyze_content.py`：POST `OLLAMA_URL`，Header 带
  `Authorization: Bearer $OLLAMA_API_KEY`，payload 含 `images:[base64...]`、
  顶层 `temperature`/`max_tokens`，`format:"json"`。
- 已用合成 fixture（6 个素材各 5 帧含中文场景文字）对真实远程 Ollama 跑了端到端验证，
  6/6 分类正确（含 C0102 经文本兜底找回「放下产品」）。
- 在 SKILL.md 增加第 6 节文档。

## 对真实产品跑 classify_materials（九日果汁）

- 输入：`/Users/chihuan/Documents/剪辑合集/冷饮/九日果汁/中间产物`（edit-dir），
  `cache/frames/` 下 21 个素材目录（每个 .MOV 一个）、187 帧。
- 结果全部写入产品中间产物目录（非 /tmp）：`material_labels.json`+`.md`、
  `cache/selected/<素材>/` 共 63 张选中帧（21×3）。
- 分类分布：拿起产品 10、饮料气泡特写 6、开盖展示 4、放下产品 1；无 needs_review。
- 验证后清理了 /tmp 下的测试夹具（vu_test/vu_dry/make_fixtures.py/classify_jiuri.log）。
- 注意：classify 真实输出始终落到 `--edit-dir`（产品中间产物目录），/tmp 里只有测试产物。

## 改 classify_materials：labels.json 按大分类驱动标签

- 用户新建 `labels.json`（skill 根目录）：`{大分类: [标签...]}`，目前含 `冷饮`、`商品` 两组。
- 脚本改动：新增 `--category`（大分类）参数，从 `labels.json` 取对应标签组；
  `--labels-file` 默认指向 skill 根 `labels.json`；新增 `--labels`（逗号分隔显式覆盖，优先级最高）。
- 标签解析优先级：`--labels` > `labels.json[category]` > 内置 DEFAULT_LABELS 兜底。
  大分类不存在时 `SystemExit(2)` 并报出可用分类；兼容旧格式（纯数组 / `{"labels":[...]}`）。
- 在 `LABEL_HINTS` 增加商品分类 4 个标签的识别要点（拆包装/倒出零食/拆袋展示/食物细节）。
- 验证：单元测试 resolve_labels（冷饮/商品/explicit/错误分类/旧格式）通过；--category 商品 dry-run 在 /tmp 副本跑通且未触碰产品目录；SKILL.md 第 6 节同步更新。

## 改 generate_strategy.py：_BEAT_PROMPT 注入大分类 labels，visual_prompt 含模型判定标签

- 目标文件是 **Documents 副本**：`/Users/chihuan/Documents/剪辑合集/skills/video-use/helpers/generate_strategy.py`
  （注意：该副本的 `labels.json` 目录名为 `冷饮`/`零食`，与 qwenpaw 副本的 `冷饮`/`商品` 不同，是用户后续改的）。
- `_BEAT_PROMPT` 原本有未填充的 `{labels}` 占位（`.format(text=text)` 会 KeyError）。现已改为含
  `{category}`/`{labels}`/`{text}` 三个占位，并新增 `label` 字段：要求模型从 labels 数组选一个动作标签，
  `visual_prompt` 必须以所选 label 开头描述画面。
- 新增 `--category` / `--labels-file`（默认 skill 根 labels.json）/ `--labels` 参数，复用
  `resolve_labels`/`load_labels_for_category`/`_normalize_action_label`（带 DEFAULT_LABELS 兜底）。
- 链路：`main`→`build_strategy`→`build_shots`→`llm_beat_and_prompt(text, model, labels, category)`，
  返回 `(beat, label, visual)`；镜头 dict 新增 `label` 字段，strategy.json 记录 `category`/`labels`，MD 表新增「动作标签」列。
- 离线（`--offline`）路径 label 留空、visual_prompt 仍由 beat 规则生成（无本地模型可判定标签）。
- 验证：_BEAT_PROMPT 格式化无残留占位、resolve_labels（冷饮/零食/explicit/错误分类）通过、离线端到端在 /tmp 跑通；
  mlx-vlm 模型路径需 Apple Silicon 环境，本机未实跑（仅验证接线）。

## 改 match_shots.py：默认走 Ollama，保留 ARK/DeepSeek 兜底

- 新增 Ollama 后端：`DEFAULT_OLLAMA_MODEL`（默认 `qwen3-vl:8b`，可由 `OLLAMA_MODEL` 覆盖）、
  `DEFAULT_OLLAMA_URL`（取 `OLLAMA_URL`）、`OLLAMA_API_KEY`（取 `OLLAMA_API_KEY`）。
- 新增 `run_ollama()`：POST `OLLAMA_URL`，Header 带 `Authorization: Bearer $OLLAMA_API_KEY`，
  payload 顶层含 `model`/`temperature`/`max_tokens`/`stream:False`/`messages:[{role:user, content}]`（纯文本无 images）。
- `main()` 默认 `--use-ollama`（BooleanOptionalAction，默认 True），用 `run_ollama`；
  传 `--no-use-ollama` 才走 `run_deepseek`（ARK）。`--ark-model` 默认值仍是 `DEFAULT_ARK_MODEL`（`deepseek-v4-flash`）。
- 新增 `--ollama-url`/`--ollama-model` 参数；最终输出打印 `backend`。仅做语法校验，未端到端实跑。

## 新增 optimize_content_report.py：回填标签 + 去 summary

- 目标文件（Documents 副本）：`/Users/chihuan/Documents/剪辑合集/skills/video-use/helpers/optimize_content_report.py`
- 功能：把 `material_labels.json` 里每个素材的 `label`（及 `confidence`/`needs_review`）回填进
  `content_report.json` 的每个 clip（按 clip_name 对应 material 名），并删除每个 clip 里冗余的 `summary` 字段。
- clip 名称（IMG_7658 / PMR00868…）与 material_labels.materials 的 key 一一对应。
- 参数：`--edit-dir`（默认从此读两个文件）/ 显式 `--content-report`+`--material-labels` / `--output`（默认覆盖输入 content_report）/ `--keep-summary` / `--no-confidence` / `--no-needs-review` / `--dry-run`。
- 验证：九日果汁真实文件 dry-run + /tmp 副本实跑均 21/21 回填、21 个 summary 删除、0 缺失；已对产品目录 content_report.json 就地跑通（顶层 fields 保留 version/model/use_ollama/clips，windows 不变）。
- 命名对应：content_report 的 clip 名 === material_labels 的 material 名（同一素材 .MOV）。
